macro decorator(dec, fn) {
    #dec(#fn)
}

macro multi_decorator(decs, fn) {
    UNWRAP(#decs).WITH(#fn)
}

// version 1
fn first_decorator(fun) {
    console.log('called before function')
    fun()
}

fn say_hello() {
    @decorator(first_decorator, fn() {
        console.log('Hello')
    })
}