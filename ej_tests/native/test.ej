import 'std'

native {
    pub fn test():int {
        return 1 + 1
    }

    pub fn test_string():string {
        msg : string = "Hello"
        return msg
    }

    pub fn new_string(message: string): string {
        return message
    }
}

dog = "Dude"

@print(await test_string())
@print(await new_string("World, $dog"))
@print(await new_string("Even more strings"))
@print(await test_string())