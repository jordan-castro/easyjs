import "std"

struct Person {
    name=null
    age=null
    
    fn constructor(self, name, age) {
        self.name = name
        self.age = age
    }

    fn greet(self) {
        console.log(self)
    }

    fn a_static_method(rg1) {
        console.log("ayo dude")
    }

    async fn grow_up(self) {
        self.age = 1 + self.age
    }
}