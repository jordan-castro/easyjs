// if using import 'j.js' will import as below
const add = (x, y) => x + y;
let t = "empty string";
1 + 1 + 1 + 1;


// if using import 'j.js' as name will import as below
const j = {
    add,
    t
};
// random script goes down here
1 + 1 + 1 + 1;
