// ! This can only be used in the browser.

fn get_elements(query) {
    elements = []
    if query.className !== undefined {
        elements.append(document.getElementsByClassName(query.className))
    }

    if query.id !== undefined {
        elements.push(document.getElementById(query.id))
    }

    if query.tagName !== undefined {
        elements.append
    }
}