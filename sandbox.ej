native {
    pub fn get_name():string {
        return "Jordan"
    }

    pub fn add_strings(string1: string):string {
        return string1 + " <- has been added to this string"
    }
}

import 'std'

@std.print(get_name())
@std.print(add_strings("This"))