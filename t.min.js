const x = 1;
let y = 2;

const add = (a, b) => {
    if (a > b) {
        return (b - a); 
    } else if (a == b) {
        return 0; 
    } else if (a) {} else {
        return (a + b); 
    }; 
};

let z = add(x, y);